
"use server"

interface ProductInCart {
    productId: number;
    quantity: number;
}

//Aqui tiene que recibir el userId: number, date: string, products: ProductInCart[]
export const addCart = async () => {

    //
    try {

        //Hacer el fetch que le pase en la image
        
    } catch (error) {
        
    }

}